// will render the chatroom component

// it uses a Chatroom/:id route and needs to use a wildcard to target and load the data of the
// selected chatroom

const ChatRoom = () => {
    
  
    return (
      <div className="createRoom-container">
        {/* use chatroom componenet to render all messages */}
        {/* use userinput component to allow users to send messages to chatroom */}
      </div>
    );
  };
  
  export default ChatRoom;